{
  "name": "obracontrol",
  "version": "0.0.0",
  "private": true,
  "description": "Ferramentas de teste e deploy do ObraControl. O APP em si continua sem build: HTML, CSS e JS servidos como arquivos estáticos. Nada aqui é empacotado no que roda no navegador.",
  "scripts": {
    "test": "npm run test:unit && npm run test:rules",
    "test:unit": "node --test tests/calc.test.cjs tests/moeda.test.cjs tests/icons.test.cjs tests/teclado.test.cjs tests/resumo.test.cjs",
    "test:rules": "firebase emulators:exec --only firestore \"node --test tests/rules.test.mjs\"",
    "rules:deploy": "firebase deploy --only firestore:rules"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^3.0.4",
    "firebase": "^10.12.2",
    "firebase-tools": "^13.15.0"
  }
}
