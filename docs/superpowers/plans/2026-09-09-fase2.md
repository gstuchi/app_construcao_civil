# Execução da Fase 2

**Status: concluída em 2026-09-09 por confirmação do usuário.** Registros de pendências abaixo preservam histórico; responsável e política definitiva seguem para Fase 4, assim como validação do compartilhamento nativo.

1. Exportação JSON/CSV e adaptador web, com testes e commit próprio.
2. Operações de conta, limpeza do cache, formulários e verificação de e-mail.
3. Fuso do perfil, expurgo de CPF em dry-run e rascunho de privacidade.
4. Testes de regressão e ciclo completo nos emuladores; registrar resultados.

Pendências externas: responsável/contato de privacidade; acesso administrativo
para conferir e expurgar CPF legado; ciclo em conta descartável de produção;
publicação e validação mobile da nova versão. Não iniciar Fase 3.

## Resultado local em 2026-09-09

- Exportação JSON/CSV implementada e validada por conteúdo de downloads reais.
- Troca de senha, verificação de e-mail não bloqueante e exclusão com confirmação
  digitada implementadas. Reautenticação incorreta não apaga documentos.
- Logout limpa IndexedDB após confirmar a fila e encerrar sessão. Recuperação de
  limpeza bloqueada tem interface própria e impede login até limpar/recarregar.
- Exclusão e logout exigem fechar outras abas do mesmo navegador. Não há trava
  distribuída entre aparelhos: Firestore e Auth não formam transação única.
  Falha após o batch é informada como exclusão parcial e admite nova tentativa.
- Perfil novo recebe tz; perfis antigos sem esse campo usam Brasília. Horários
  do cron continuam 9h/18h de Brasília; somente a data do resumo segue o perfil.
- CPF já estava ausente do cadastro. Script paginado foi testado no emulador:
  dry-run preserva dados, --apply remove somente CPF e nova consulta encontra zero.
- Rascunho de privacidade atualizado, preservando contato Instagram existente.
  Responsável ainda precisa ser confirmado; contato informado e incluído na política: suportecustta@gmail.com.
- `tests/browser/fase2.cjs`: ciclo completo com Auth/Firestore reais emulados,
  incluindo zero documentos e usuário após exclusão; temas claro/escuro nos
  viewports 414 e 1440px. Logout com outra aba bloqueado; após fechá-la, conclui.
- `tests/browser/persistencia.cjs`: duas escritas offline sobrevivem ao fechamento,
  sobem ao reconectar e logout conclui com limpeza. `tests/browser/sync.cjs`: passou.
- `npm test`: passou, incluindo novos testes de conta, exportação e fuso e os
  33 testes das rules existentes. Nenhuma alteração das rules nesta entrega.

### Pendências para declarar Fase 2 encerrada

1. Conferir regras implantadas e executar ciclo em **conta nova descartável** de
   produção, sem usar conta do usuário real para testar exclusão.
2. Executar dry-run administrativo dos CPFs em produção; revisar quantidade e
   aplicar expurgo. Nenhum CPF real foi consultado ou removido nesta implementação.
3. Confirmar responsável e novo e-mail; revisar e publicar política definitiva
   após congelamento de SDKs na Fase 4, conforme planejamento original.
4. Publicar commits e testar nova versão v35 no iPhone, incluindo compartilhar
   arquivos pelo share sheet do Safari. Testes Chromium não substituem esse teste.

### Comandos

```powershell
npm test
node tests/browser/servidor.cjs
# Em outro terminal, com Playwright disponível no NODE_PATH:
node tests/browser/sync.cjs
npx firebase emulators:exec --only firestore "node tests/browser/persistencia.cjs"
npx firebase emulators:exec --config firebase.test.json --project demo-custta-phase2 --only firestore,auth "node tests/browser/fase2.cjs"
# Administrativo: exige credencial Admin configurada, nunca adicionada ao Git.
node notificacoes/scripts/purga-cpf.mjs --project app-construcao-civil
# Após revisar o dry-run, --apply executa a remoção de CPF.
```

## Atualização informada pelo usuário em 2026-09-09

- Usuário relatou publicação e funcionamento da exclusão após roteiro de conferência em Authentication e Firestore. Console não foi inspecionado pelo agente.
- Após orientação para conferir perfis no Firestore, informou ausência de CPF. Pendência de CPF resolvida conforme relato; nenhum expurgo em produção foi executado.
- E-mail público confirmado: suportecustta@gmail.com. Incluído nos contatos e solicitações de exclusão da política; cache atualizado para v37.
- Pendências anteriores de contato e CPF ficam substituídas por este registro. Falta confirmar nome do responsável e, se ainda não coberto pelo relato mobile, compartilhamento nativo no iPhone/Safari. Política definitiva segue prevista após congelamento de SDKs na Fase 4.
